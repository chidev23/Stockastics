import { Ionicons } from '@expo/vector-icons';
import { router, useLocalSearchParams } from 'expo-router';
import { Pressable, ScrollView, Text, View } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';

export default function NewsArticleScreen() {
  const { title = 'Market News' } = useLocalSearchParams<{ title?: string }>();
  return <SafeAreaView className="flex-1 bg-white" edges={['top']}><View className="flex-row items-center border-b border-slate-100 px-5 py-4"><Pressable onPress={()=>router.back()} className="mr-3 h-10 w-10 items-center justify-center rounded-full bg-slate-50"><Ionicons name="arrow-back" size={20} color="#0F172A"/></Pressable><Text className="text-lg font-extrabold text-slate-900">News</Text></View><ScrollView contentContainerClassName="px-5 pb-10 pt-6"><Text className="text-xs font-bold uppercase tracking-wider text-blue-700">Market News</Text><Text className="mt-3 text-3xl font-extrabold leading-9 text-slate-900">{title}</Text><Text className="mt-3 text-xs text-slate-500">STOCKASTICS News · Just now</Text><View className="mt-6 h-44 items-center justify-center rounded-3xl bg-slate-100"><Ionicons name="newspaper-outline" size={48} color="#64748B"/></View><Text className="mt-6 text-base leading-7 text-slate-700">This article view is ready for live news content from the configured market-news provider. The production feed can supply the headline, source, timestamp, related stocks and full article content without changing the mobile UI.</Text><View className="mt-6 rounded-2xl bg-blue-50 p-4"><Text className="font-bold text-blue-900">Market context</Text><Text className="mt-1 text-sm leading-5 text-blue-800">Related ticker, country, sector and sentiment metadata can be displayed here when supplied by the news API.</Text></View></ScrollView></SafeAreaView>}
