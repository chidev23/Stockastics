import { router } from 'expo-router'; import { Pressable, ScrollView, Text, View } from 'react-native'; import { SafeAreaView } from 'react-native-safe-area-context'; const c=[['Retail','/retail-signals'],['IPO','/ipo-signals'],['Buyback','/buyback-signals'],['Sentiment','/sentiment-signals'],['Ex-Div','/ex-dividend-signals'],['Income','/income-signals']] as const; export default function Screen(){return <SafeAreaView className="flex-1 bg-slate-50"><View className="px-5 pt-5"><Pressable onPress={()=>router.back()}><Text className="text-blue-700">‹ Back</Text></Pressable><Text className="mt-3 text-2xl font-extrabold text-slate-900">Buyback Signals</Text></View><ScrollView horizontal showsHorizontalScrollIndicator={false} className="mt-5 max-h-16" contentContainerClassName="gap-2 px-5">{c.map(([l,r],i)=><Pressable key={r} onPress={()=>router.push(r as never)} className={`rounded-full px-4 py-3 ${i===2?'bg-blue-700':'bg-white border border-slate-200'}`}><Text className={`text-xs font-bold ${i===2?'text-white':'text-slate-700'}`}>{l}</Text></Pressable>)}</ScrollView><View className="mx-5 mt-5 rounded-2xl bg-white p-5"><Text className="font-extrabold text-slate-900">Buyback opportunities</Text><Text className="mt-2 text-slate-500">Companies announcing or executing share repurchase activity will appear here.</Text></View></SafeAreaView>}
